trigger:
  branches:
    include:
    - master

pr:
  branches:
    include:
    - master
    - feature/*

jobs:
- job: WebApp
  pool:
    vmImage: windows-2019 # https://docs.microsoft.com/en-us/azure/devops/pipelines/agents/hosted?view=azure-devops
  steps:
  - template: build/azure-pipelines-steps.aspnetcore.yml
    parameters:
     toolPath: '$(Build.BinariesDirectory)'
     dotnetInstallConfigFile: 'NuGet-PublicOnly.config' # workaround for a bug in dotnet cli https://github.com/dotnet/cli/issues/9586#issuecomment-484585146
     gitVersionVersion: '5.0.0-beta5.9' # release latest version here https://www.nuget.org/packages/GitVersion.CommandLine.DotNetCore
     nugetVersion: '5.0.2' # see recommended version here https://www.nuget.org/downloads
     buildConfiguration: 'Release'
     deployPackage: true
     deployFiles: true
     outputDirectory: $(Build.ArtifactStagingDirectory)
